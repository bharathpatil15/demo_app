<h1>Listing customer_bills</h1>
<table>
  <tr>
    <th>Customer</th>
    <th>Total value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @customer_bills.each do |customer_bill| %>
  <tr>
    <td><%= customer_bill.customer.name %></td>
    <td><%= customer_bill.total_value %></td>
    <td><%= link_to 'Show', customer_bill %></td>
    <td><%= link_to 'Edit', edit_customer_bill_path(customer_bill) %></td>
    <td><%= link_to 'Destroy', customer_bill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Customer bill', new_customer_bill_path %> | <br/>
<%= link_to 'Customer', customers_path %> 
